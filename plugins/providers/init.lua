-- Bundled provider manifests. Each manifest calls
-- `maki.provider.register` as a side effect of loading this module, so
-- adding a provider here is a single `require` line (and a sibling file).

require("deepseek")
